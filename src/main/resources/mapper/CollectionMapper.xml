<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tencent.wxcloudrun.dao.CollectionMapper">

   <!-- <resultMap id="countersMapper" type="com.tencent.wxcloudrun.model.CollectionModel">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="nameId" column="name_id"/>
        <result property="fullName" column="full_name"/>
        <result property="desc" column="desc"/>
    </resultMap>

    <select id="getCounter" resultMap="countersMapper" parameterType="java.lang.Integer">
        SELECT `id`, `count`, `createdAt`, `updatedAt`
        FROM Counters
        WHERE id = #{id}
    </select>

    <delete id="clearCount" parameterType="java.lang.Integer">
        DELETE
        FROM Counters
        where id = #{id}
        limit 1
    </delete>

    <update id="upsertCount" parameterType="com.tencent.wxcloudrun.model.Counter">
        INSERT INTO `Counters`(`id`, `count`)
        VALUE(#{id}, #{count})
        ON DUPLICATE KEY UPDATE count=#{count}
    </update>-->

</mapper>
